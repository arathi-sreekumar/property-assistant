import React from "react";
type HelpProps = {
    title: string;
    children: React.ReactNode;
};
export declare const Help: ({ title, children }: HelpProps) => import("react/jsx-runtime").JSX.Element;
export {};
